## API Report File for "@resili/llm"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Context } from '@resili/core';
import { EventHandler } from '@resili/core';
import { FailureClassifier } from '@resili/core';
import { MetricsRecorder } from '@resili/core';
import { ResiliConfig } from '@resili/core';
import { ResiliEventType } from '@resili/core';
import { Unsubscribe } from '@resili/core';

// @public
export interface BudgetAccountant {
    getAccumulatedMicroUsd(scope: string): number;
    getReservedMicroUsd(scope: string): number;
    reserve(scope: string, estimatedMicroUsd: number, maxAccumulatedMicroUsd?: number): boolean;
    settle(scope: string, reservedMicroUsd: number, actualMicroUsd: number): number;
}

// @public
export type BudgetDecision = {
    readonly allowed: true;
} | {
    readonly allowed: false;
    readonly limitKind: "per-request" | "accumulated";
    readonly limitMicroUsd: number;
};

// @public
export interface BudgetDecisionInput {
    // (undocumented)
    readonly accumulatedMicroUsd: number;
    // (undocumented)
    readonly estimatedCostMicroUsd: number;
    // (undocumented)
    readonly maxAccumulatedCostMicroUsd?: number;
    // (undocumented)
    readonly maxCostPerRequestMicroUsd?: number;
    // (undocumented)
    readonly reservedMicroUsd?: number;
}

// @public
export interface BudgetGuardOptions {
    readonly accountant?: BudgetAccountant;
    readonly maxAccumulatedCostUsd?: number;
    readonly maxCostPerRequestUsd?: number;
    readonly onUnknownPricing?: UnknownPricingBehavior;
    readonly scope?: string | BudgetScopeResolver;
    readonly warningThresholdRatio?: number;
}

// @public
export type BudgetLimitKind = "per-request" | "accumulated" | "unknown-pricing";

// @public
export type BudgetScopeResolver = (request: LlmRequest, ctx: Context) => string;

// @public
export function calculateCost(usage: LlmUsage, rate: PricingRate): LlmCost;

// @public
export function createLlmClient(options: CreateLlmClientOptions): LlmClient;

// @public
export interface CreateLlmClientOptions extends Omit<ResiliConfig<LlmResponse>, "metrics"> {
    // (undocumented)
    readonly budget?: BudgetGuardOptions;
    readonly metrics?: MetricsRecorder;
    // (undocumented)
    readonly model?: string;
    // (undocumented)
    readonly pricing?: PricingResolver;
    // (undocumented)
    readonly provider: LlmProvider;
}

// @public
export function createMemoryBudgetAccountant(): BudgetAccountant;

// @public
export function createPricingResolver(rows: readonly ModelPricing[]): PricingResolver;

// @public
export function defineProvider(provider: LlmProvider): LlmProvider;

// @public
export function evaluateBudget(input: BudgetDecisionInput): BudgetDecision;

// @public
export function isLlmError(error: unknown): error is LlmError;

// @public
export function isLlmErrorRetryable(error: unknown): boolean;

// @public
export const LLM_METRIC_NAMES: Readonly<{
    requests: "resili_llm_requests_total";
    failures: "resili_llm_failures_total";
    budgetRejections: "resili_llm_budget_rejections_total";
    latencyMs: "resili_llm_latency_ms";
    inputTokens: "resili_llm_input_tokens_total";
    outputTokens: "resili_llm_output_tokens_total";
    tokens: "resili_llm_tokens_total";
    costMicroUsd: "resili_llm_cost_micro_usd_total";
    streams: "resili_llm_streams_total";
    streamFailures: "resili_llm_stream_failures_total";
    streamDurationMs: "resili_llm_stream_duration_ms";
    streamTtftMs: "resili_llm_stream_ttft_ms";
    streamChunks: "resili_llm_stream_chunks_total";
    streamOutputTokens: "resili_llm_stream_output_tokens_total";
}>;

// @public
export class LlmBudgetExceededError extends LlmError {
    constructor(options: {
        readonly scope: string;
        readonly limitMicroUsd: number;
        readonly accumulatedMicroUsd: number;
        readonly attemptedMicroUsd: number;
        readonly limitKind: "per-request" | "accumulated" | "unknown-pricing";
        readonly provider?: string;
        readonly model?: string;
        readonly cause?: unknown;
    });
    readonly accumulatedMicroUsd: number;
    readonly attemptedMicroUsd: number;
    readonly limitKind: "per-request" | "accumulated" | "unknown-pricing";
    readonly limitMicroUsd: number;
    readonly scope: string;
}

// @public
export const llmClassifier: FailureClassifier;

// @public
export interface LlmClient {
    // (undocumented)
    destroy(): Promise<void>;
    // (undocumented)
    generate(request: LlmGenerateRequest): Promise<LlmGenerateResult>;
    // (undocumented)
    on<T extends LlmEventType>(type: T, handler: LlmEventHandler<T>): LlmUnsubscribe;
    // (undocumented)
    onCore<T extends ResiliEventType>(type: T, handler: EventHandler<T>): Unsubscribe;
    // (undocumented)
    stream(request: LlmGenerateRequest): LlmStream;
}

// @public
export interface LlmCost {
    // (undocumented)
    readonly currency: "USD";
    // (undocumented)
    readonly inputCostMicroUsd: number;
    // (undocumented)
    readonly inputCostUsd: number;
    // (undocumented)
    readonly model: string;
    // (undocumented)
    readonly outputCostMicroUsd: number;
    // (undocumented)
    readonly outputCostUsd: number;
    // (undocumented)
    readonly provider: string;
    // (undocumented)
    readonly totalCostMicroUsd: number;
    // (undocumented)
    readonly totalCostUsd: number;
}

// @public
export class LlmError extends Error {
    constructor(classification: LlmErrorClassification, options?: LlmErrorOptions);
    readonly classification: LlmErrorClassification;
    readonly code: LlmErrorCode;
    readonly isResiliLlm = true;
    readonly model?: string;
    readonly provider?: string;
    readonly retryable: boolean;
    readonly retryAfterMs?: number;
}

// @public
export type LlmErrorClassification = "authentication" | "authorization" | "invalid_request" | "rate_limited" | "timeout" | "provider_unavailable" | "overloaded" | "context_limit_exceeded" | "content_policy" | "network_transient" | "budget" | "unknown";

// @public
export type LlmErrorCode = "ERR_LLM_AUTH" | "ERR_LLM_FORBIDDEN" | "ERR_LLM_INVALID_REQUEST" | "ERR_LLM_RATE_LIMITED" | "ERR_LLM_TIMEOUT" | "ERR_LLM_UNAVAILABLE" | "ERR_LLM_OVERLOADED" | "ERR_LLM_CONTEXT_LIMIT" | "ERR_LLM_CONTENT_POLICY" | "ERR_LLM_NETWORK" | "ERR_LLM_BUDGET" | "ERR_LLM_UNKNOWN";

// @public
export interface LlmErrorOptions {
    // (undocumented)
    readonly cause?: unknown;
    // (undocumented)
    readonly message?: string;
    // (undocumented)
    readonly model?: string;
    // (undocumented)
    readonly provider?: string;
    // (undocumented)
    readonly retryable?: boolean;
    // (undocumented)
    readonly retryAfterMs?: number;
}

// @public
export type LlmEvent = LlmEventMap[LlmEventType];

// @public
export interface LlmEventBase {
    // (undocumented)
    readonly model: string;
    // (undocumented)
    readonly operationName: string;
    // (undocumented)
    readonly provider: string;
    // (undocumented)
    readonly requestId: string;
    // (undocumented)
    readonly timestamp: number;
    // (undocumented)
    readonly type: LlmEventType;
}

// @public
export type LlmEventHandler<T extends LlmEventType> = (event: LlmEventMap[T]) => void;

// @public
export interface LlmEventMap {
    // (undocumented)
    readonly LlmBudgetRejected: LlmEventBase & {
        readonly type: "LlmBudgetRejected";
        readonly scope: string;
        readonly limitKind: "per-request" | "accumulated" | "unknown-pricing";
        readonly accumulatedMicroUsd: number;
        readonly attemptedMicroUsd: number;
        readonly limitMicroUsd: number;
    };
    // (undocumented)
    readonly LlmBudgetWarning: LlmEventBase & {
        readonly type: "LlmBudgetWarning";
        readonly scope: string;
        readonly accumulatedMicroUsd: number;
        readonly limitMicroUsd: number;
    };
    // (undocumented)
    readonly LlmRequestCompleted: LlmEventBase & {
        readonly type: "LlmRequestCompleted";
        readonly durationMs: number;
        readonly inputTokens: number;
        readonly outputTokens: number;
        readonly totalTokens: number;
        readonly costMicroUsd?: number;
    };
    // (undocumented)
    readonly LlmRequestFailed: LlmEventBase & {
        readonly type: "LlmRequestFailed";
        readonly durationMs: number;
        readonly classification: string;
        readonly retryable: boolean;
    };
    // (undocumented)
    readonly LlmRequestStarted: LlmEventBase & {
        readonly type: "LlmRequestStarted";
    };
    // (undocumented)
    readonly LlmStreamCompleted: LlmEventBase & {
        readonly type: "LlmStreamCompleted";
        readonly durationMs: number;
        readonly ttftMs?: number;
        readonly chunkCount: number;
        readonly inputTokens: number;
        readonly outputTokens: number;
        readonly totalTokens: number;
        readonly costMicroUsd?: number;
    };
    // (undocumented)
    readonly LlmStreamFailed: LlmEventBase & {
        readonly type: "LlmStreamFailed";
        readonly durationMs: number;
        readonly classification: string;
        readonly retryable: boolean;
        readonly committed: boolean;
    };
    // (undocumented)
    readonly LlmStreamStarted: LlmEventBase & {
        readonly type: "LlmStreamStarted";
    };
    // (undocumented)
    readonly LlmUsageRecorded: LlmEventBase & {
        readonly type: "LlmUsageRecorded";
        readonly inputTokens: number;
        readonly outputTokens: number;
        readonly totalTokens: number;
        readonly costMicroUsd?: number;
    };
}

// @public
export type LlmEventType = "LlmRequestStarted" | "LlmRequestCompleted" | "LlmRequestFailed" | "LlmUsageRecorded" | "LlmBudgetWarning" | "LlmBudgetRejected" | "LlmStreamStarted" | "LlmStreamCompleted" | "LlmStreamFailed";

// @public
export type LlmFinishReason = "stop" | "length" | "content_filter" | "tool_calls" | "error" | "unknown";

// @public
export interface LlmGenerateRequest {
    // (undocumented)
    readonly estimatedInputTokens?: number;
    // (undocumented)
    readonly estimatedOutputTokens?: number;
    // (undocumented)
    readonly input: string;
    // (undocumented)
    readonly metadata?: Readonly<Record<string, string>>;
    // (undocumented)
    readonly model?: string;
    // (undocumented)
    readonly signal?: AbortSignal;
}

// @public
export interface LlmGenerateResult {
    // (undocumented)
    readonly cost?: LlmCost;
    // (undocumented)
    readonly response: LlmResponse;
    // (undocumented)
    readonly usage: LlmUsage;
}

// @public
export interface LlmProvider {
    execute(request: LlmRequest, ctx: Context): Promise<LlmResponse>;
    readonly name: string;
    stream?(request: LlmRequest, ctx: Context): Promise<AsyncIterable<LlmProviderStreamFrame>>;
}

// @public
export interface LlmProviderIdentity {
    readonly name: string;
}

// @public
export interface LlmProviderStreamFrame {
    readonly finishReason?: LlmFinishReason;
    readonly model?: string;
    readonly text?: string;
    readonly usage?: Partial<LlmUsage>;
}

// @public
export interface LlmRequest {
    readonly estimatedInputTokens?: number;
    readonly estimatedOutputTokens?: number;
    readonly input: string;
    readonly metadata?: Readonly<Record<string, string>>;
    readonly model: string;
    readonly provider: string;
}

// @public
export interface LlmResponse {
    readonly content: string;
    readonly finishReason: LlmFinishReason;
    readonly model: string;
    readonly provider: string;
    readonly usage: LlmUsage;
}

// @public
export interface LlmStream {
    // (undocumented)
    [Symbol.asyncIterator](): AsyncIterator<LlmStreamEvent, undefined>;
    // (undocumented)
    result(): Promise<LlmStreamResult>;
}

// @public
export interface LlmStreamCompleted {
    // (undocumented)
    readonly cost?: LlmCost;
    // (undocumented)
    readonly finishReason: LlmFinishReason;
    // (undocumented)
    readonly model: string;
    // (undocumented)
    readonly provider: string;
    // (undocumented)
    readonly type: "completed";
    // (undocumented)
    readonly usage: LlmResponse["usage"];
}

// @public
export type LlmStreamEvent = LlmStreamTextDelta | LlmStreamCompleted;

// @public
export interface LlmStreamResult {
    // (undocumented)
    readonly cost?: LlmCost;
    // (undocumented)
    readonly finishReason: LlmFinishReason;
    // (undocumented)
    readonly model: string;
    // (undocumented)
    readonly provider: string;
    // (undocumented)
    readonly usage: LlmResponse["usage"];
}

// @public
export interface LlmStreamTextDelta {
    // (undocumented)
    readonly text: string;
    // (undocumented)
    readonly type: "text-delta";
}

// @public
export type LlmUnsubscribe = () => void;

// @public
export interface LlmUsage {
    readonly dimensions?: Readonly<Record<string, number>>;
    readonly inputTokens: number;
    readonly outputTokens: number;
    readonly totalTokens: number;
}

// @public
export function microUsdToUsd(microUsd: number): number;

// @public
export interface ModelPricing {
    // (undocumented)
    readonly inputPerMillionTokensUsd: number;
    // (undocumented)
    readonly model: string;
    // (undocumented)
    readonly outputPerMillionTokensUsd: number;
    // (undocumented)
    readonly provider: string;
}

// @public
export function normalizeUsage(usage: Partial<LlmUsage> | undefined): LlmUsage;

// @public
export interface PricingRate {
    // (undocumented)
    readonly inputMicroUsdPerMillionTokens: number;
    // (undocumented)
    readonly model: string;
    // (undocumented)
    readonly outputMicroUsdPerMillionTokens: number;
    // (undocumented)
    readonly provider: string;
}

// @public
export interface PricingResolver {
    // (undocumented)
    resolve(provider: string, model: string): PricingRate | undefined;
}

// @public
export const TOKENS_PER_MILLION = 1000000;

// @public
export type UnknownPricingBehavior = "allow" | "reject";

// @public
export const USD_MICROS = 1000000;

// @public
export function usdToMicroUsd(usd: number): number;

// (No @packageDocumentation comment for this package)

```
